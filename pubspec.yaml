name: ai_health_guardian
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.12.0
  google_fonts: ^8.0.2
  flutter_svg: ^2.2.4
  http: ^1.6.0
  google_mlkit_text_recognition: ^0.15.1
  image_picker: ^1.2.1
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  geolocator: ^10.1.0
  fl_chart: ^0.70.2
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0
  animated_text_kit: ^4.2.2
  shimmer: ^3.0.0
  camera: ^0.11.1
  permission_handler: ^11.3.1
  sensors_plus: ^6.1.1
  share_plus: ^10.1.4
  path_provider: ^2.1.5
  url_launcher: ^6.2.0
  local_auth: ^2.3.0
  flutter_bloc: ^9.0.0
  equatable: ^2.0.7

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
